[app]
name = "header-echo"
version = "0.1.0"
description = "Echo request headers for debugging the OpenHost router contract"

[runtime.container]
image = "Dockerfile"
port = 8080

[resources]
memory_mb = 128
cpu_millicores = 250
